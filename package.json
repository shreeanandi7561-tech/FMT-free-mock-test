{
  "name": "fmt-free-mock-test",
  "version": "1.0.0",
    "type": "module",
  "scripts": {
    "start": "vercel dev",
    "build": "echo 'No build step needed for static site'"
    "engines": {
    "node": "18.x"
  }
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  }
}
